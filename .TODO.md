- If disliked by me, remove it.
- Don't allow landscape.
- Notify players if they are away and a new question is revealed.


- Host auto start timer when game is full: When game is full, start a 5 second timer to auto start the game.
- rank based on score - more than one player can have same rank.
- Tell category agent to split ties with shower thoughts
- Ring color of host when they submit - turns from host to self
- When host leaves, self-ring of new host doesn't update color.
- Submitted score to match the mirror's value.
- Main button to be disabled for all cards except last in review mode for host.
- Last question's unit not fading away indicators.
- moving with player at reveal time.

## Guesstimate Pricing Model
-   Currency: Fermi (₣)
-   $1 = ₣100
-   ₣1 = $0.01



You are a Flutter engineer and I need you to Implement the frontend part of the Daily Question feature.

Background:
- Familiarize yourself with the project's backend @README.md and frontend @README.md 
- Recent commits added the Daily Question backend feature, with notes on Frontend Integration:@implementation_plan.md.resolved 
- The frontend now does not support multiple modes visually. The  @main_screen.dart has a single game settings card for the "Party" mode. 
- I am testing things on the emulator (see @Makefile run-frontend command) before deployment.

Additional Requirements:
- If we are missing important backend features to implenent the daily question properly, write a feature request I can pass tp my backend agent.
- Use the app's central theme @app_theme.dart 
- Add a Daily Question card in @main_screen.dart 
- Study Firestore usage patterns and best practices. 
- Update frontend docs as needed @README.md @apps/fermi-frontend/docs 
- Make the necessary changes so I can test the Daily Question feature locally on the emulator. This requires setting up the db properly and simulating calling the backend endpoints to set the daily question stuff (what the scheduled cloud run job does).  
- All frontend tests should pass.