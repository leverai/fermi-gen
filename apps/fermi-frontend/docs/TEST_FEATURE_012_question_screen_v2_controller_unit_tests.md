## TEST_FEATURE_012 (deprecated) – `QuestionScreenV2Controller` unit tests

### Scope

> **Deprecated:** This feature has been split into smaller tasks.
> Do not use this file for new work. Instead, use:
>
> - `TEST_FEATURE_012A` – state & index unit tests
> - `TEST_FEATURE_012B` – answer & timers unit tests
> - `TEST_FEATURE_012C` – reveal, players & review unit tests

Originally, this feature described implementing all `QuestionScreenV2Controller` unit tests from `docs/TESTS.md`:

- **Unit Tests → Controllers → `QuestionScreenV2Controller`**

This content is kept only for historical reference. All actionable work has moved to `012A`, `012B`, and `012C`.
